import React from 'react'
import classNames from 'classnames/bind'
import styles from './VentureAgent.module.scss'

const cx = classNames.bind(styles);

const VentureAgent = () => {
  return (
    <div>VentureAgent</div>
  )
}

export default VentureAgent